#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef double db;
typedef pair<int,int> PII;


    
void solve() { 
    137488;
    ll t = 1;
    while(15 * t % 687440 != 0 || 17 * t % 466666 != 0) {
        t++;
    }
    ll a = 727998960, b = 825065488;
    db ans = sqrt(a * a + b * b);
    cout << fixed << setprecision(5) << ans;
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);

    solve();
    return 0;
}
    
