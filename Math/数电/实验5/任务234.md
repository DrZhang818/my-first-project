# 任务2：观察该计数器的计数时钟是上升沿还是下降沿触发。

* 上升沿触发

# 任务3：验证RCO输出端与计数值QD~QA的逻辑关系。

* RCO是进位输出。对于$74160$，当计数器计到$9$（即$Q_DQ_CQ_BQ_A = 1001$）且ENT为高电平时，RCO会输出高电平

# 任务4：通过实验，说明74160的ENP和ENT的作用。

* ENP：计数使能端。当ENP $=0$时，计数器保持当前数值不变，但RCO仍受ENT控制
* ENT：进位使能端。当 ENT=0 时，计数器保持不变，且 RCO 强制为低电平（切断进位链）。